# Leave unset for local dev — Vite's dev server proxies /api to
# localhost:5000 automatically (see vite.config.js).
#
# For production builds (Vercel/Netlify), set this to your deployed
# backend's URL, including the /api suffix:
# VITE_API_URL=https://your-backend.onrender.com/api
